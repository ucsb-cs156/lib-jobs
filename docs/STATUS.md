# lib-jobs adoption status

Which app repos are on which version of `lib-jobs`, as of 2026-09-24.
Generated by checking each repo's `pom.xml` on `main` directly (`gh api
repos/ucsb-cs156/<repo>/contents/pom.xml?ref=main`) plus its merged PRs —
not derived from the phase checklist in the top-level `CLAUDE.md`, which is
narrative and can drift out of sync with actual repo state.

| Repo | lib-jobs version | Status | Notes |
|---|---|---|---|
| `proj-dining` | v0.4.0 | 🔄 v0.4.1 PR open | v0.3.3 bump [#148](https://github.com/ucsb-cs156/proj-dining/pull/148) merged 2026-08-25; v0.4.0 bump [#156](https://github.com/ucsb-cs156/proj-dining/pull/156) merged 2026-09-23; v0.4.1 bump [#158](https://github.com/ucsb-cs156/proj-dining/pull/158) open. History: pilot for both phase 2 (backend install) and the v0.2.0 job-log redesign. PR [#132](https://github.com/ucsb-cs156/proj-dining/pull/132) merged. v0.3.2 bump PR [#147](https://github.com/ucsb-cs156/proj-dining/pull/147) merged 2026-08-24: no app-code changes needed for cancellation itself (TestJob has no silent loop; dining has no frontend Jobs UI, tested via curl/Swagger). Proactively added RestTemplate timeouts to all 3 construction sites per the standing audit item, even though no job calls them today. 186 tests, jacoco 100%, pitest 218/218. Live-verified working (Phill, via Swagger) before merge; third app on v0.3.x. Defensive v0.3.3 bump PR [#148](https://github.com/ucsb-cs156/proj-dining/pull/148) open 2026-08-25 — dining doesn't chain jobs, pure pom.xml version bump. 186 tests, jacoco 100%. |
| `proj-scaffold` | v0.4.0 | ⚠️ needs v0.4.1 bump | v0.3.3 bump [#123](https://github.com/ucsb-cs156/proj-scaffold/pull/123) merged 2026-09-21. [#129](https://github.com/ucsb-cs156/proj-scaffold/pull/129) merged 2026-09-23 with the title "Bump lib-jobs to v0.4.1" but its diff is `v0.3.3` → **`v0.4.0`** (retitled in place, pom never retargeted), so scaffold is on v0.4.0 and still needs a one-line v0.4.1 PR. History: PR [#118](https://github.com/ucsb-cs156/proj-scaffold/pull/118) merged 2026-08-17 (v0.2.0). PR [#121](https://github.com/ucsb-cs156/proj-scaffold/pull/121) merged 2026-08-22 (v0.3.2): Cancel UI, RestTemplate timeout fix, and checkCancellation() checkpoints in the sync job's silent loops — bumped through v0.3.0 → v0.3.1 → v0.3.2 in place as live QA testing surfaced each issue. Live-verified on dokku QA: cancel-while-running, cancel-while-queued, the startup-recovery sweep, and cancel-during-a-silent-unchanged-heavy-resync all confirmed working. First app on v0.3.x. Defensive v0.3.3 bump PR [#123](https://github.com/ucsb-cs156/proj-scaffold/pull/123) open 2026-08-25 — scaffold doesn't chain jobs, pure pom.xml version bump. 833 tests, jacoco 100%; frontend 706 tests. |
| `proj-courses` | v0.4.0 | 🔄 v0.4.1 PR open | v0.3.3 bump [#337](https://github.com/ucsb-cs156/proj-courses/pull/337) merged 2026-08-25; v0.4.0 bump [#352](https://github.com/ucsb-cs156/proj-courses/pull/352) merged 2026-09-23; v0.4.1 bump [#354](https://github.com/ucsb-cs156/proj-courses/pull/354) open (restores its "See entire log" link, see v0.4.1 below). History: PR [#321](https://github.com/ucsb-cs156/proj-courses/pull/321) merged 2026-08-19 (v0.2.0), after Liquibase-infrastructure prep in [#316](https://github.com/ucsb-cs156/proj-courses/pull/316)/[#318](https://github.com/ucsb-cs156/proj-courses/pull/318)/[#319](https://github.com/ucsb-cs156/proj-courses/pull/319)/[#324](https://github.com/ucsb-cs156/proj-courses/pull/324). PR [#331](https://github.com/ucsb-cs156/proj-courses/pull/331) merged 2026-08-24 (v0.3.2, straight from v0.2.0): Cancel UI, RestTemplate timeouts on 4 construction sites, checkCancellation() checkpoints in UpdateCourseDataJob/GradeHistoryImportServiceImpl, and a real JobCancelledException-wrapping bug fix. Deploy blocked by an unrelated pre-existing Liquibase checksum mismatch (introduced by PR #330, hit live on courses-qa); fixed and merged separately as [#333](https://github.com/ucsb-cs156/proj-courses/pull/333) using the validCheckSums precedent from #324. Second app on v0.3.x. Defensive v0.3.3 bump PR [#337](https://github.com/ucsb-cs156/proj-courses/pull/337) open 2026-08-25 — courses runs `jobsExecutor` at pool-size 2 but doesn't chain jobs today (confirmed via grep), so this closes the gap in advance; pure pom.xml version bump. 424 tests, jacoco 100%; frontend 669 tests. |
| `proj-citelines` | v0.4.0 | 🔄 v0.4.1 PR open | v0.4.0 bump [#139](https://github.com/ucsb-cs156/proj-citelines/pull/139) merged 2026-09-23; v0.4.1 bump [#141](https://github.com/ucsb-cs156/proj-citelines/pull/141) open. History: PR [#97](https://github.com/ucsb-cs156/proj-citelines/pull/97) merged 2026-08-19. Added to the rollout 2026-08-16 (not one of the original five forks). v0.3.3 bump PR [#125](https://github.com/ucsb-cs156/proj-citelines/pull/125) merged 2026-08-25: bumped straight to v0.3.3 (skipping v0.3.2) since citelines is about to build job chaining for issue #110 part 2, which v0.3.3 makes safe. Branched pre-#119 (the BibTexEntryUpgrade→Improve rename) and rebased once it merged, so the checkCancellation() checkpoint targets the renamed class. RestTemplate timeout fix needed a redo after breaking every @WebMvcTest slice (RestTemplateBuilder isn't provided there) — fixed via SimpleClientHttpRequestFactory directly. Backend 713 tests, jacoco 100%, pitest 926/926; frontend 542 tests. Fifth app on v0.3.x, merged first among the batch — and the only one that actually needed v0.3.3, not just defensively. |
| `proj-happycows` | v0.4.0 | ⚠️ needs v0.4.1 bump | Migrated off homegrown jobs code to lib-jobs v0.1.6 via PR [#270](https://github.com/ucsb-cs156/proj-happycows/pull/270), merged 2026-08-08, then frozen (mission-critical during the CHEM 123 game) until mid-September 2026 while every other app took v0.2.0 through v0.3.3. Freeze over: PR [#338](https://github.com/ucsb-cs156/proj-happycows/pull/338) merged 2026-09-24 (closes [#337](https://github.com/ucsb-cs156/proj-happycows/issues/337); [#336](https://github.com/ucsb-cs156/proj-happycows/issues/336) was a duplicate scoping of the same work and is closed) took it **straight from v0.1.6 to v0.4.0** in one PR, on top of its Spring Boot 3.5.16 / Java 25 migration ([#335](https://github.com/ucsb-cs156/proj-happycows/pull/335)). Schema: the scaffold/courses stage-then-complete backfill pair around the library's `002-job-logs-table.json` changeset (app changeset `014` stages `jobs.log` into a temp column via `includeAll`; `002` included explicitly next; app changeset `015`, kept in a separate `changes-post-lib-jobs/` directory so `includeAll` cannot pull it in early, moves the staged text into `job_logs` one row per historical job and drops the temp column). Verified against a real pre-upgrade DB, not just a fresh one: booted `main` at v0.1.6 on a file H2 DB, seeded three legacy `jobs` rows (multi-line log, NULL log, empty log) via the H2 shell, booted the branch on the same file with `ddl-auto=validate` — exactly one `job_logs` row, for the multi-line job, timestamped with its `updated_at`; `LOG`/`LOG_BACKFILL` gone. Frontend: Cancel button on `PagedJobsTable` (the live table; its `JobsTable` is story-only) and a "See entire log" link on every row to a new `AdminJobLogPage` at `/admin/jobs/logs/:id` (the courses precedent), since `/paginated` has only returned a 10-line preview since v0.2.0. Standing checklist: no RestTemplate/HttpClient sites in `src/main`, no chaining (`runAsJob` only from controller endpoints and `@Scheduled` methods), every job loop logs each iteration so no `checkCancellation()` needed; the 11 job test classes use the preserved `new JobContext(null, job)` seam and passed unchanged. No Java source changes. Backend 459 tests, jacoco 100%, pitest 670/670; frontend 912 tests, 100% coverage, Stryker 100% on the two changed files (two equivalent `StringLiteral` mutants on react-table column ids annotated — an empty `id` falls back to `Header`). Only CI hiccups: two equivalent Stryker mutants (annotated) and an incremental-pitest job marked *cancelled* after every step, including 670/670 pitest, had succeeded (concurrency cancel on the gh-pages deploy; clean on re-run). **Still needs the one-line v0.4.1 bump** — #338 targeted v0.4.0, so its new full-log link is unconditional rather than gated on the truncation marker. |
| `proj-frontiers` | v0.4.0 | ⚠️ needs v0.4.1 bump | v0.3.3 bump [#701](https://github.com/ucsb-cs156/proj-frontiers/pull/701) merged 2026-09-06. [#777](https://github.com/ucsb-cs156/proj-frontiers/pull/777) merged 2026-09-23 with the title "Bump lib-jobs to v0.4.1" but its diff is `v0.3.3` → **`v0.4.0`** (same retitle-without-retarget as scaffold #129), so frontiers is on v0.4.0 and still needs a one-line v0.4.1 PR; its separate full-log-page UI fix is tracked in `CLAUDE.md`. History: PR [#694](https://github.com/ucsb-cs156/proj-frontiers/pull/694) merged 2026-08-21. First-ever lib-jobs adoption combined with the Course→scope migration (DESIGN.md §3.4) in one PR, landing directly on v0.2.0 — the last app in the original six-app rollout. v0.3.2 bump PR [#700](https://github.com/ucsb-cs156/proj-frontiers/pull/700) merged 2026-08-25: biggest surface area of the v0.3.x rollout so far — checkCancellation() checkpoints added to 6 of 16 job classes (10 checkpoints total, including PullTeamsFromGithubJob's per-member forEach lambda), RestTemplate timeouts on all 5 construction sites, and a Cancel button added to JobsTable, wired into both AdminJobsPage and the per-course JobTabComponent. Backend 707 tests, jacoco 100%, pitest 1120/1120; frontend 515 tests, 100% coverage. Fourth app on v0.3.x. Defensive v0.3.3 bump PR [#701](https://github.com/ucsb-cs156/proj-frontiers/pull/701) open 2026-08-25 — none of frontiers' 16 job classes chain jobs (confirmed via grep), pure pom.xml version bump. 707 tests, jacoco 100%; frontend 515 tests, 100% coverage. |

## Library release status

- **v0.4.1** (log-preview truncation marker: `getJobLogPreview` prefixes
  `... N earlier lines omitted (T lines total)` when a preview is
  truncated — lib-jobs issue #4 / PR #5, tagged 2026-09-23, verified on
  JitPack) is the current released version. **No app is on it yet as of
  2026-09-24.** Bump PRs open: citelines #141, courses #354, dining #158.
  Scaffold #129 and frontiers #777 were retitled to v0.4.1 while open but
  merged with `v0.4.0` in the pom (see their rows above), and happycows'
  #338 targeted v0.4.0 — so scaffold, frontiers, and happycows each still
  need a fresh one-line v0.4.1 PR.
- **v0.4.0** (Spring Boot 3.5.16 / Java 25 build; class-file major 69, so
  v0.3.3 is the last release a Java 21 app can load — lib-jobs issue #2 /
  PR #3, tagged 2026-09-23). **All six apps are on v0.4.0 on `main` as of
  2026-09-24**: citelines #139, courses #352, dining #156, scaffold #129,
  frontiers #777 (one-line bumps, all merged 2026-09-23), and happycows
  #338 (the whole deferred v0.1.6 → v0.4.0 migration, merged 2026-09-24).
  This completes the original six-app rollout: **every app is now on the
  job_logs schema with cancellation and startup recovery.**
- **v0.3.3** (fixes a job-chaining transaction-visibility race in
  `JobService.runAsJob` — the initial `queued` row's save now runs in its
  own `REQUIRES_NEW` transaction, same idiom already used for log writes
  and cancellation re-fetches) was the current released version until v0.4.0, tagged
  2026-08-25 and verified resolvable on JitPack. Surfaced proactively by a
  session working on citelines issue #110 (job chaining is part 2 of that
  work, not yet built) before any app hit it live — full analysis at
  `docs/job-chaining-analysis.md`, resolved version in `DESIGN.md` §10.
  Not hypothetical: courses already runs `jobsExecutor` at pool-size 2, so
  it was one un-reviewed chaining call site away from hitting this for
  real. Empirically verified with a hand-verified red/green integration
  test (fails with a clean 10-second timeout when reverted). **Scaffold,
  courses, dining, and frontiers are fully adopted on v0.3.2** (PR #121,
  PR #331, PR #147, and PR #700 respectively, all merged and verified);
  none of them chain jobs today, so v0.3.2 remained correct for them —
  **citelines bumped straight to v0.3.3** (PR #125, merged) since it's
  the one about to build chaining. Phill then asked to close the gap
  everywhere rather than leaving it latent: checked each of the other
  four apps for the chaining pattern (`git grep runAsJob`/`JobService`
  against each app's own jobs package) and confirmed none currently chain
  — courses' `jobsExecutor` pool-size-2 config was the one that prompted
  the question, but its two `runAsJob` call sites are a `@Scheduled` cron
  method and app-startup hooks, neither inside another job's transaction.
  All four then took a **defensive v0.3.3 bump** anyway (dining #148 and
  courses #337 merged 2026-08-25, frontiers #701 2026-09-06, scaffold #123
  2026-09-21) — pure `pom.xml` version bumps, no app-code changes, since
  none of them had anything to fix.
- **v0.3.2** (`JobContext.checkCancellation()` made public — same
  re-fetch-and-throw behavior as the check already inside `log()`, but
  callable directly with no log line written) — superseded by v0.3.3 for
  any app that chains jobs or might soon; still correct and current for
  every app that doesn't. Built 2026-08-21, the same day as v0.3.1, after
  live QA testing of v0.3.1's own timeout fix on scaffold surfaced a third
  issue: a job body with a loop that only logs on specific branches (the
  common case being silent) never gave cancellation a checkpoint to fire
  from during that silence — see `CLAUDE.md`'s v0.3.2 entry for the full
  incident.
- **v0.3.1** (startup recovery sweep — `@EventListener(ApplicationReadyEvent.class)`
  on `JobService` marks any job still `queued`/`running`/`cancelling` at
  boot as a new terminal status, `interrupted`; zero app wiring required)
  — superseded by v0.3.2 the same day.
- **v0.3.0** (job cancellation — `POST /api/jobs/{id}/cancel`, cooperative
  cancellation checked on every `ctx.log()` call, `docs/DESIGN.md` §9) —
  superseded by v0.3.2/v0.3.3; no app should adopt v0.3.0 or v0.3.1 alone.
- **v0.2.0** (job-log storage redesign — normalized `job_logs` table,
  `/paginated` filtering, incremental log tailing) — superseded by v0.3.x
  (happycows skipped it, going v0.1.6 → v0.4.0 directly in #338).

**Rollout order for v0.3.x adoption** (Phill, 2026-08-21): citelines →
scaffold → frontiers → dining → courses. happycows excluded — frozen until
~2026-09-15 (see the table above). **Order adjusted twice since:** citelines
was mid-deploy, so scaffold went first (2026-08-21, done); then citelines
had unrelated cleanup work in progress, so courses went next (2026-08-22,
done 2026-08-24); dining picked up next and finished 2026-08-24 (PR #147);
frontiers picked up next and finished 2026-08-25 (PR #700, citelines
still had unrelated cleanup in progress); citelines picked up last and
finished 2026-08-25 (PR #125, straight to v0.3.3). **The v0.3.x rollout
was fully merged for every app except happycows by 2026-08-25**, and the
four defensive v0.3.3 bumps followed (last one scaffold #123, 2026-09-21).
**happycows joined on 2026-09-24** via #338, going straight from v0.1.6 to
v0.4.0 once its freeze expired and its own Java 25 migration (#335) had
landed — see its row above for the backfill and verification details.

**Standing action items for every v0.3.x rollout PR** (Phill, 2026-08-21),
both found during scaffold's live QA testing and unrelated to lib-jobs
itself, but newly consequential now that jobs run on a single-threaded
executor by default:
1. Audit that app's own blocking HTTP calls (RestTemplate, HttpClient,
   etc.) for missing timeouts — an untimed call can wedge the entire
   queue forever, and only a genuine restart (not cancellation) recovers
   it.
2. Check that app's own job bodies for a loop that only calls
   `ctx.log(...)` on specific branches (e.g. a "skip"/"error" case, with
   the common case being silent) — such a loop never gives cancellation a
   checkpoint to fire from during its silent stretches. Add
   `ctx.checkCancellation()` (v0.3.2+) once per iteration where found.

Scaffold hit both for real on QA and fixed them in #121 alongside its
v0.3.0→v0.3.2 bump; courses, frontiers, and citelines each hit both
proactively (fixed before any live bug report) in #331, #700, and #125
respectively — frontiers the largest instance of item 2 (6 job classes,
10 total checkpoints); citelines' item 1 needed a mid-course correction
after the first RestTemplate timeout attempt broke every `@WebMvcTest`
slice (see its STATUS.md row above). happycows (#338) needed neither: no
HTTP call sites at all, and every job loop already logs each iteration.

## Keeping this up to date

Re-run the check above after any app-repo PR that bumps (or newly adds) the
`lib-jobs` dependency:

```bash
for repo in proj-citelines proj-courses proj-dining proj-frontiers proj-happycows proj-scaffold; do
  echo "=== $repo ==="
  gh api "repos/ucsb-cs156/$repo/contents/pom.xml?ref=main" --jq '.content' 2>/dev/null \
    | base64 -d 2>/dev/null | grep -A2 "artifactId>lib-jobs<"
done
```
